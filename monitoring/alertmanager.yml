global:
  resolve_timeout: 5m

route:
  receiver: "default"
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: "default"

# Alert rules are defined in monitoring/prometheus_rules.yml
# and referenced from prometheus.yml
